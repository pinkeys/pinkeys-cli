language: python
branches:
  only:
    - master
python:
  - 3.6
install:
  pip install -r requirements.txt
script:
  - python manage.py test
  - pytest
after_success:
  - test $TRAVIS_BRANCH = "master" &&